<!DOCTYPE html>
<html>

<head>
    <title>AI Prompts for Teachers</title>
    <style>
        @import url(https://fonts.googleapis.com/css?family=Montserrat:400,700);
        @import url('https://fonts.googleapis.com/css2?family=Raleway:wght@400;500;600&display=swap');

        body {
            background: #D9D9D9;
        }

        h1,
        form {
            max-width: 420px;
            margin: 15px auto;
        }

        h1 {
            color: #0D0D0D;
            font-family: 'Montserrat', Arial, Raleway, sans-serif;
            font-weight: 700;
            font-size: 24px;
        }

        label {
            color: #0D0D0D;
            font-family: Raleway, Arial, sans-serif;
            font-weight: 700;
            font-size: 18px;
        }

        textarea {
            color: #0D0D0D;
            font-family: Raleway, Arial, sans-serif;
            font-weight: 500;
            font-size: 18px;
            border-radius: 5px;
            line-height: 22px;
            background-color: transparent;
            border: 2px solid #078C4E;
            transition: all 0.3s;
            padding: 13px;
            margin-bottom: 15px;
            width: 100%;
            box-sizing: border-box;
            outline: 0;
        }

        .prompt-input:focus {
            border: 2px solid #CC4949;
        }

        .small-textarea {
            height: 60px;
            line-height: 150%;
            resize: vertical;
        }

        .large-textarea {
            height: 150px;
            line-height: 150%;
            resize: vertical;
        }

        .choice-box {
            border-radius: 5px;
            background-color: transparent;
            border: 2px solid #078C4E;
            transition: all 0.3s;
            padding: 13px;
            margin-bottom: 5px;
            width: 100%;
            box-sizing: border-box;
            outline: 0;
        }

        [type="submit"],
        .enabled {
            font-family: 'Montserrat', Arial, Helvetica, sans-serif;
            width: 100%;
            background: #078C4E;
            border-radius: 5px;
            border: 0;
            cursor: pointer;
            color: white;
            font-size: 24px;
            padding-top: 10px;
            padding-bottom: 10px;
            transition: all 0.3s;
            box-shadow: 0px 0px 10px #404040;
            margin-top: 20px;
            font-weight: 700;
        }

        .active:active {
            box-shadow: inset 0px 2px 3px rgba(0, 0, 0, 0.2);
        }

        .disabled {
            font-family: 'Montserrat', Arial, Helvetica, sans-serif;
            width: 100%;
            background: #404040;
            border-radius: 5px;
            border: 0;
            cursor: pointer;
            color: white;
            font-size: 24px;
            padding-top: 10px;
            padding-bottom: 10px;
            transition: all 0.3s;
            box-shadow: 0px 0px 10px #404040;
            margin-top: 20px;
            font-weight: 700;
        }

        .hover:hover {
            background: #0D734D;
        }

        #aiPrompt {
            color: #D9D9D9;
            font-family: Raleway, Arial, sans-serif;
            font-weight: 700;
            font-size: 16px;
            max-width: 420px;
            margin: 50px auto;
            background-color: #0D0D0D;
            border-radius: 10px;
            box-shadow: 0px 0px 15px #404040;
            padding: 50px 13px;
            margin-top: 20px;
        }

        .hidden {
            display: none;
        }
    </style>
</head>

<body>
    <div class="prompt-box">
        <h1 class="typed-out">Lesson Plan Generator</h1>
        <form id="prompt-form">
            <label>Grade: <br>
                <textarea class="small-textarea" type="text" id="grade" placeholder="7th"></textarea>
            </label><br>
            <label>Subject: <br>
                <textarea class="small-textarea" type="text" id="subject" placeholder="math"></textarea>
            </label><br>
            <label>Assessment Topic: <br>
                <textarea class="small-textarea" type="text" id="topic" placeholder="ratios"></textarea>
            </label><br>
            <label>Question Types (Can select multiple):</label><br>
            <div class="choice-box">
                <input type="checkbox" id="type1" name="type1" value="multiple choice">
                <label for="type1">Multiple Choice</label><br>
                <input type="checkbox" id="type2" name="type2" value="word problem">
                <label for="type2">Word Problems</label><br>
                <input type="checkbox" id="type4" name="type4" value="open response">
                <label for="type4">Open Response</label><br>
                <input type="checkbox" id="type5" name="type5" value="fill in the blank">
                <label for="type5">Fill in the Blank</label><br>
                <input type="checkbox" id="type6" name="type6" value="true or false">
                <label for="type6">True or False</label><br>
            </div><br>
            <label>Question Difficulty: </label><br>
            <div class="choice-box">
                <input type="radio" id="easy" name="difficulty" value="easy">
                <label for="easy">Easy</label><br>
                <input type="radio" id="medium" name="difficulty" value="medium">
                <label for="medium">Medium</label><br>
                <input type="radio" id="hard" name="difficulty" value="hard">
                <label for="hard">Hard</label><br>
                <input type="radio" id="mixed" name="difficulty" value="mixed">
                <label for="mixed">Mixed</label><br>
            </div><br>
            <label>Quantity of Questions: <br>
                <textarea class="small-textarea" type="text" id="quantity" placeholder="10"></textarea>
            </label><br>
            <button class="hover active" type="submit" id="generate-prompt-button">Generate Prompt</button>
            <button type="button" id="copy-prompt-button" class="disabled" disabled>Copy Prompt and Open
                ChatGPT</button>
        </form>
        <div class="container">
            <p id="aiPrompt"></p>
        </div>

    </div>
    <script>
        const form = document.getElementById("prompt-form");
        const grade = document.getElementById("grade");
        const subject = document.getElementById("subject");
        const topic = document.getElementById("topic");
        const quantity = document.getElementById("quantity");
        const promptDisplay = document.getElementById("aiPrompt");
        const copyPromptButton = document.getElementById("copy-prompt-button");

        function typeWriter(txt) {
            var i = 0;
            var speed = 25;
            function type() {
                if (i < txt.length) {
                    document.getElementById("aiPrompt").innerHTML += txt.charAt(i);
                    i++;
                    setTimeout(type, speed);
                }
            }
            type();
        }

        function handleSubmit(event) {
            event.preventDefault();

            // Get the selected question types
            const questionTypes = [];
            const checkboxes = document.querySelectorAll('input[type="checkbox"]:checked');
            for (const checkbox of checkboxes) {
                questionTypes.push(checkbox.value);
            }

            // Get the selected difficulty
            const difficulty = document.querySelector('input[type="radio"]:checked').value;

            // Check if all required fields are filled out
            if (grade.value === "" || subject.value === "" || topic.value === "" || questionTypes.length === 0 || difficulty === "" || quantity.value === "") {
                alert("Please fill out all the fields.");
                return;
            }

            // Generate the prompt
            const prompt = `Make me a ${quantity.value} question assessment for ${grade.value} students in my ${subject.value} class on the topic of ${topic.value}. The assessment should be of ${difficulty} difficulty. Make sure to include a variety of these question types: ${questionTypes.join(", ")}. Please include the answers.`;

            // Enable the copy prompt button
            copyPromptButton.disabled = false;
            copyPromptButton.classList.remove("disabled");
            copyPromptButton.classList.add("enabled");
            copyPromptButton.classList.add("hover");
            copyPromptButton.classList.add("active");

            // Display the prompt with the typewriter animation
            typeWriter(prompt);
        }

        function copyPrompt() {
            // Get the text field
            var copyText = document.getElementById("aiPrompt");

            // Select the text field
            var range = document.createRange();
            range.selectNode(copyText);
            window.getSelection().removeAllRanges();
            window.getSelection().addRange(range);

            // Copy the selected text
            document.execCommand("copy");

            // Alert the copied text
            alert("Copied the prompt");

            // Open a link to chat.openai.com
            window.open("http://chat.openai.com", "_blank");
        }

        form.addEventListener("submit", handleSubmit);
        copyPromptButton.addEventListener("click", copyPrompt);
    </script>
</body>

</html>
