<!DOCTYPE html>
<html>

<head>
    <title>AI Prompts for Teachers</title>
    <style>
        @import url(https://fonts.googleapis.com/css?family=Montserrat:400,700);
        @import url('https://fonts.googleapis.com/css2?family=Raleway:wght@400;500;600&display=swap');

        body {
            background: #D9D9D9;
        }

        h1,
        form {
            max-width: 420px;
            margin: 15px auto;
        }

        h1 {
            color: #0D0D0D;
            font-family: 'Montserrat', Arial, Raleway, sans-serif;
            font-weight: 700;
            font-size: 24px;
        }

        label {
            color: #0D0D0D;
            font-family: Raleway, Arial, sans-serif;
            font-weight: 700;
            font-size: 18px;
        }

        textarea {
            color: #0D0D0D;
            font-family: Raleway, Arial, sans-serif;
            font-weight: 500;
            font-size: 18px;
            border-radius: 5px;
            line-height: 22px;
            background-color: transparent;
            border: 2px solid #078C4E;
            transition: all 0.3s;
            padding: 13px;
            margin-bottom: 15px;
            width: 100%;
            box-sizing: border-box;
            outline: 0;
        }

        .prompt-input:focus {
            border: 2px solid #CC4949;
        }

        .small-textarea {
            height: 60px;
            line-height: 150%;
            resize: vertical;
        }

        .large-textarea {
            height: 150px;
            line-height: 150%;
            resize: vertical;
        }

        .choice-box {
            border-radius: 5px;
            background-color: transparent;
            border: 2px solid #078C4E;
            transition: all 0.3s;
            padding: 13px;
            margin-bottom: 5px;
            width: 100%;
            box-sizing: border-box;
            outline: 0;
        }

        [type="submit"],
        .enabled {
            font-family: 'Montserrat', Arial, Helvetica, sans-serif;
            width: 100%;
            background: #078C4E;
            border-radius: 5px;
            border: 0;
            cursor: pointer;
            color: white;
            font-size: 24px;
            padding-top: 10px;
            padding-bottom: 10px;
            transition: all 0.3s;
            box-shadow: 0px 0px 10px #404040;
            margin-top: 20px;
            font-weight: 700;
        }

        .active:active {
            box-shadow: inset 0px 2px 3px rgba(0, 0, 0, 0.2);
        }

        .disabled {
            font-family: 'Montserrat', Arial, Helvetica, sans-serif;
            width: 100%;
            background: #404040;
            border-radius: 5px;
            border: 0;
            cursor: pointer;
            color: white;
            font-size: 24px;
            padding-top: 10px;
            padding-bottom: 10px;
            transition: all 0.3s;
            box-shadow: 0px 0px 10px #404040;
            margin-top: 20px;
            font-weight: 700;
        }

        .hover:hover {
            background: #0D734D;
        }

        #aiPrompt {
            color: #D9D9D9;
            font-family: Raleway, Arial, sans-serif;
            font-weight: 700;
            font-size: 16px;
            max-width: 420px;
            margin: 50px auto;
            background-color: #0D0D0D;
            border-radius: 10px;
            box-shadow: 0px 0px 15px #404040;
            padding: 50px 13px;
            margin-top: 20px;
        }

        .hidden {
            display: none;
        }
    </style>
</head>

<body>
    <div class="prompt-box">
        <h1>Assessment Generator</h1>
        <form id="prompt-form">
            <label>Grade:</label><br>
            <textarea class="small-textarea" type="text" id="grade" placeholder="6th"></textarea><br>
            <label>Subject:</label><br>
            <textarea class="small-textarea" type="text" id="subject" placeholder="World History"></textarea><br>
            <label>Essential Standard:</label><br>
            <textarea class="large-textarea" type="text" id="standard"
                placeholder="H.13.6.10 Examine key concepts and influences of major belief systems on societies"></textarea><br>
            <label>Topic:</label><br>
            <textarea class="small-textarea" type="text" id="topic" placeholder="Abrahamic religions"></textarea><br>
            <label>Learning Objective (Students will be able...):</label><br>
            <textarea class="large-textarea" type="text" id="objective"
                placeholder="to compare and contrast the origins, basic beliefs, and practices of Judaism, Christianity, and Islam "></textarea><br>
            <label>Assessment Method:</label><br>
            <div class="choice-box">
                <input type="radio" id="observation" name="assessment" value="observation">
                <label for="observation">Observation</label><br>
                <input type="radio" id="exit-ticket" name="assessment" value="exit-ticket">
                <label for="exit-ticket">Exit Ticket</label><br>
                <input type="radio" id="quiz" name="assessment" value="quiz">
                <label for="quiz">Quiz</label><br>
                <input type="radio" id="assignment" name="assessment" value="assignment">
                <label for="assignment">Turned in Assignment</label><br>
            </div><br>
            <label>Time (minutes)<br>
                <textarea class="small-textarea" type="text" id="time" placeholder="50"></textarea>
            </label><br>
            <button class="hover active" type="submit" id="generate-prompt-button">Generate Prompt</button>
            <button type="button" id="copy-prompt-button" class="disabled" disabled>Copy Prompt and Open
                ChatGPT</button>
        </form>
        <div class="container">
            <p id="aiPrompt"></p>
        </div>

    </div>
    <script>
        const form = document.getElementById("prompt-form");
        const grade = document.getElementById("grade");
        const subject = document.getElementById("subject");
        const standard = document.getElementById("standard");
        const objective = document.getElementById("objective");
        const topic = document.getElementById("topic");
        const time = document.getElementById("time");
        const promptDisplay = document.getElementById("aiPrompt");
        const copyPromptButton = document.getElementById("copy-prompt-button");

        function typeWriter(txt) {
            var i = 0;
            var speed = 25;
            function type() {
                if (i < txt.length) {
                    document.getElementById("aiPrompt").innerHTML += txt.charAt(i);
                    i++;
                    setTimeout(type, speed);
                }
            }
            type();
        }

        function handleSubmit(event) {
            event.preventDefault();

            // Get the selected question types
            const questionTypes = [];

            // Get the selected assessment method
            const assessment = document.querySelector('input[type="radio"]:checked').value;

            // Check if all required fields are filled out
            if (grade.value === "" || subject.value === "" || standard.value === "" || objective.value === "" || topic.value === "" || assessment === "" || time.value === "") {
                alert("Please fill out all the fields.");
                return;
            }

            // Generate the prompt
            const prompt = `Write me a lesson plan on ${topic.value} for a ${grade.value} grade ${subject.value} class. The essential standard for this unit is "${standard.value}". At the end of the ${time.value} minute lesson, students should be able ${objective.value}. Please include this assessment method: ${assessment}. Additionally, detail possible extension and intervention opportunities.`;

            // Enable the copy prompt button
            copyPromptButton.disabled = false;
            copyPromptButton.classList.remove("disabled");
            copyPromptButton.classList.add("enabled");
            copyPromptButton.classList.add("hover");
            copyPromptButton.classList.add("active");

            // Display the prompt with the typewriter animation
            typeWriter(prompt);
        }

        function copyPrompt() {
            // Get the text field
            var copyText = document.getElementById("aiPrompt");

            // Select the text field
            var range = document.createRange();
            range.selectNode(copyText);
            window.getSelection().removeAllRanges();
            window.getSelection().addRange(range);

            // Copy the selected text
            document.execCommand("copy");

            // Alert the copied text
            alert("Copied the prompt");

            // Open a link to chat.openai.com
            window.open("http://chat.openai.com", "_blank");
        }

        form.addEventListener("submit", handleSubmit);
        copyPromptButton.addEventListener("click", copyPrompt);
    </script>
</body>

</html>
